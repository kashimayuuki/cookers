<div class='container'>
  <div class='row'>
    <div class='col-md-8 offset-2'>
      <h3 class = "my-4 mt-5">ログイン</h3>
      <div class = "field text-center md-4">
        <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, class: "btn btn-secondary sign_in", method: :post %>
      </div>

      <p class="text-center mb-4">会員の方はこちらからログイン</p>

      <%= form_with model:resource, as: resource_name, url: session_path(resource_name) do |f| %>
      <table class="table table-borderless table-sm">
        <div class="field">
          <tr>
            <td><%= f.label :"メールアドレス" %></td>
            <td><%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-40" %></td>
          </tr>
        </div>

        <div class="field">
          <tr>
            <td><%= f.label :"パスワード"%></td>
            <td><%= f.password_field :password, autocomplete: "current-password", class: "w-40" %></td>
          </tr>
        </div>

        <div class="actions">
          <tr>
            <td></td>
            <td><%= f.submit "ログイン",class: "btn btn-primary px-5 mt-4" %></td>
          </tr>
        </div>
      </table>
      <% end %>
      <h4 class="text-right my-4 mt-5">会員登録がお済みでない方</h4>
      <div class="text-right">
        <%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。
      </div>
    </div>
  </div>
</div>