<table class="table table-hover">
  <!--<thead class="thead-light">-->
  <thead>
    <tr>
      <th class= "text-center" >ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
    <tr>
      <th class= "text-center" ><%= link_to genre.name, menus_path(genre_id: genre.id) %></th>
    </tr>
    <% end %>
  </tbody>
</table>

